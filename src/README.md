# src

Purpose
- Core application code and utilities (PDF extraction, pipeline runner, exporters).

What’s here
- `pdf_extractor.py`, `run_pipeline.py`, `export.py`, and graph/edge utilities.

Notes
- `src/run_pipeline.py` can be used to run end-to-end jobs; many higher-level scripts import helpers from `src/`.

